const INITIAL_STATE = {
    categories:[]
}

const CategoryReducer=(state=INITIAL_STATE, action) =>{
    switch(action.type){
        default:
            return state
    }
}

export default CategoryReducer